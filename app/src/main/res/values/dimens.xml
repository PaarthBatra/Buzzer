<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ── Screen Padding ──────────────────────────────────────────── -->
    <dimen name="screen_padding">20dp</dimen>

    <!-- ── Spacing Scale ───────────────────────────────────────────── -->
    <dimen name="spacing_xsmall">4dp</dimen>
    <dimen name="spacing_small">8dp</dimen>
    <dimen name="spacing_medium">16dp</dimen>
    <dimen name="spacing_large">24dp</dimen>
    <dimen name="spacing_xlarge">40dp</dimen>

    <!-- ── Typography ──────────────────────────────────────────────── -->
    <dimen name="text_size_caption">12sp</dimen>
    <dimen name="text_size_body">14sp</dimen>
    <dimen name="text_size_subtitle">16sp</dimen>
    <dimen name="text_size_title">22sp</dimen>
    <dimen name="text_size_headline">28sp</dimen>
    <dimen name="text_size_buzz">36sp</dimen>

    <!-- ── Buzz Button ─────────────────────────────────────────────── -->
    <dimen name="buzz_button_size">240dp</dimen>
    <dimen name="buzz_button_corner">120dp</dimen>

    <!-- ── Cards / Surfaces ────────────────────────────────────────── -->
    <dimen name="card_corner_radius">12dp</dimen>
    <dimen name="card_elevation">4dp</dimen>

    <!-- ── Input Fields ────────────────────────────────────────────── -->
    <dimen name="input_corner_radius">8dp</dimen>
    <dimen name="input_padding_vertical">12dp</dimen>
    <dimen name="input_padding_horizontal">16dp</dimen>

</resources>
